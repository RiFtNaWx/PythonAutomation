---
keywords: [a19, a20, a21, paste-values, dut-grid, sts-pdf, datasheet-fetch, rs2323, rs3213, apply-owner-pic]
main_idea: Excel fill is DUT+channel grids on live RS622 (GBW R20:U20). Missing SKU PDFs download into local Reference only. STS PDF is a column table. Setup Apply-only stays; Lim header follows SeeLim inventory pic.
---

# 2026-09-11 Handover A19 DUT fill + A20 PDF/STS + A21 pic

PREFLIGHT: PARTIAL. Reuse: 2026-09-11_handover-lookup-a19.md, 2026-09-11_family-pdf-class.md, 2026-09-10_operator-ux-leftovers.md. Spawn: skip.

## A19

Live TTSOP8 `sheet_map` maps GBW DUT columns R20:U20 / AA20:AD20, VOS R16:U16, Noise VN_OUT_PP_mV R16:U16.
`session_values` expands list + CHA/CHB. `record_step` stores channel.
VOS/VOH/VOL/I+ now return spec ids (`VOS_mV`, `VOH_*`, `IPLUS_uA`) so START matches limits, not `fit.vos_mV`.

ORT still photos-only (no fake us). Logic VOH sheet has no numeric anchors yet.

## A20

`fetch_and_store` searches `www.run-ic.com/en/search.html` (not the JS homepage on en.run-ic.com), then saves `RS2323_(RevWeb).pdf` and `RS3213_(RevWeb).pdf` under local Reference. Never into `#Test_Database`.
Family PDFs RS22X/RS32X stay unused for switch/LDO. RS2323 RON typ 0.6; RS3213 IQ typ 30 uA.
Logic extracts can stamp `ICC_uA` max 1 uA + VCC range. STS `datalog.pdf` is landscape columns (Parameter..Result), not a markdown dump.

## A21

Combo change still Apply-only. Invalid `if !campaignKnown` fixed.
`applyOwner` prefers inventory pic folder (Lim -> SeeLim for RS2323) but keeps a disk package that already exists (MSOP not empty UQFN).
Photo hint suffixes ORT on non-OpAmp. Remaining punch-list rows 1-5, 7-10 already in app.js.

## Proof

```
python -m ate.core.check_session_values
python -m ate.core.check_specs_datalog
python -m ate.core.check_lookup
python -m ate.core.check_ui_contract
```
