---
keywords: r-0003, a07-t01, a08-t01, a09-t01, photo-layout, paste.photos, mapped_dc, dmm, vohl, logic-campaign, rs29511, rs1g08, leftover-honest
main_idea: Independent R-0003 on A07/A08/A09. A07 and A09 assertions hold on disk+checks (no worker/UI). A08 coded claims hold; leftover-honest is PSRR/CMRR/AOL mapped captures not full physics. No FAIL for parent to patch.
---

PREFLIGHT: PARTIAL
reuse: docs/subagents_findings/2026-09-03_a07-photo-layout-preview.md, 2026-09-03_a08-mapped-tests-dmm.md, 2026-09-03_a09-logic-campaign-platform.md, 2026-09-03_a06-t01-r0003-verify-pass.md
spawn: skip (this run is the verifier)

Verifier did not implement. Did not close tickets. Did not start worker 8766 or UI 5174. Did not unpark A13/A14. Did not invent Excel cells. Did not write live sheet_map. R-0007 poisons were restored; post-restore checks green.

Law read: NETIE.md section 5; ALWAYS_UPDATE_PARALLEL_SCALE.md; ticket acceptance first (not the diff). No repo CLAUDE.md. Local tickets have no GitHub issue numbers in this repo.

# Verdict table (return to parent)

| Ticket | Verdict | Parent must fix? |
|--------|---------|------------------|
| A07-T01 | PASS | no |
| A08-T01 | leftover-honest | no (allowed leftover) |
| A09-T01 | PASS | no |

Finding: `docs/subagents_findings/2026-09-13_a07-a09-r0003-verify.md`

Exact verify-agent lines:

```
PASS               A07-T01  assertion verified live: python -m ate.reporting.check_photo_layout -> OK photo-layout... EXIT:0; resolve_shot_file refuses .. and non-screenshots/graphs; layout_preview reconstructed SlewRate latest+previous from Test_Database screenshots/; Results grid in ate/ui/web
leftover-honest    A08-T01  coded assertions hold (DMM on session, no stubs.py, vohl missing DMM via runner formula, mapped captures not RuntimeError); leftover = PSRR/CMRR/AOL still mapped captures not full physics
PASS               A09-T01  assertion verified live: python -m ate.core.check_logic_campaign -> OK... EXIT:0; Logic/RS29511/Soo + Logic/RS1G08/Ariff trees; Soo-only vs Ariff DC ids; fixture LOGIC not G11
```

HTTP GET /shot was not live-hit (worker stays down per instruction). Refuse gate is the same function GET /shot calls.

# Commands (venv; cwd short path EUGENE~1\PYTHON~1)

## A07 named checks

```
python -m ate.reporting.check_photo_layout
OK photo-layout: merged-box discover, no _PHOTO_TTSOP, YAML patch keeps comments
EXIT:0

python -m ate.core.check_family_load
OK opamp=17 logic=34 level=34 switch=4 power=6 demo=1 restored=17 tests (no cross-family leak); family-scoped catalog/timing OK
EXIT:0

python -m ate.core.check_lab_report_sync
OK lab-report sync: workbook, sheet_map, and OpAmp lab_sheet agree
EXIT:0
```

## A08 named check

```
python -m ate.core.check_mapped_tests
OK mapped-tests: every sheet_map test has a spec; DMM classifies; no stubs
EXIT:0
```

## A09 named check

```
python -m ate.core.check_logic_campaign
OK logic-campaign: RS29511 + RS1G08 + RS0204 sheet_map / enabled_tests / LOGIC fixture / A12 Ariff ids
EXIT:0
```

Post R-0007 restore (mapped_dc + photo_layout) both mapped-tests and photo-layout still EXIT:0.

# A07-T01 -- PASS

Acceptance (paraphrase): paste.photos via photo_layout; no hardcoded _SSSR/_LSSR/_SETTLING maps in lab_report.py; Results grid from graphs/screenshots; GET /shot refuses outside campaign screenshots/graphs; named checks green.

Independent probes (not the check's word):

- `lab_report.py` has none of `_SSSR_PHOTO_ANCHORS`, `_LSSR_PHOTO_ANCHORS`, `_SETTLING_UNIT_COLS`, `_SETTLING_BODY_ROW`. No A91/A84 literals. Imports `photo_anchor`. `place_sssr_photos` / `place_lssr_photos` call `_place_mapped_photo` with sheet_map keys SmallSignalStep / LargeSignalStep.
- `resolve_shot_file("../secret.png")` -> PermissionError bad rel (not FileNotFoundError). Same for `..\`, `screenshots/../../secret.png`, empty rel.
- Real campaign files refused: `_manifest/sheet_map.yaml` and `workbook/RS622XK_Lab_Report_TTSOP.xlsx` -> PermissionError not a graph/screenshot.
- `layout_preview()` default: 14 photo tests from Eugene RS622 sheet_map. SlewRate: 8 cells, 8 latest + 8 previous from `SlewRate/DUT_1/screenshots/...` (kind=screenshot). GBW and SettlingTime also had latest+previous pairs.
- Results UI exists in source: `#layout-grid`, `#layout-compare`, overlay checkbox; `rpc("layout_preview")` / `rpc("save_photo_layout")`. Worker GET `/shot` calls `resolve_shot_file` and maps PermissionError -> 403.

Cite:

- `ate/reporting/lab_report.py` 12, 176-271
- `ate/reporting/photo_layout.py` 136-162, 237-294, 376-394
- `ate/worker/server.py` 935-949
- `ate/ui/web/index.html` 404-429
- `ate/ui/web/app.js` 3573-3669
- `ate/reporting/check_photo_layout.py` 14-42, 126-136

R-0007: appended `_SSSR_PHOTO_ANCHORS = {}` to lab_report.py; check returned `lab_report.py still hardcodes _SSSR_PHOTO_ANCHORS`; restored; check errors []. Gate can fail.

## A07 check does NOT prove

- Live operator click on :5174 (UI not started).
- Live HTTP 403 from GET /shot on :8766 (worker not started). Function refuse + handler mapping were read and the function was executed.
- Live Excel `embed_photo` into a workbook.
- Save layout writing the live campaign yaml (YAML patch was on a sample string; comments kept).
- Graphs/ preferred over screenshots when both exist (this campaign's hits were screenshots/).
- Check's traversal clause also accepts FileNotFoundError; this run got PermissionError so the function is fail-closed. The check itself is slightly weak.

Leftover (not FAIL): `photo_anchor()` still falls back to `sheet_layout.ort_photo_anchor_map()` / `A46` when yaml+live discover miss an ORT key (`photo_layout.py` 153-156). Primary ORT preview used yaml cell A44. Ticket banned the second map in `lab_report.py`, which is gone.

# A08-T01 -- leftover-honest

Acceptance: Discover classifies DMM; `instr.dmm` optional at open; every sheet_map excel_sheet has a TestSpec not from stubs.py; VOL fails without DMM; PSRR/CMRR/AOL/EMIRR/Noise/PowerOn capture instead of stub RuntimeError; check_mapped_tests green. Honesty leftover allowed: PSRR/CMRR/AOL still mapped captures.

Independent probes:

- `classify_idn` DMM6500 / 34461A / 34401A / Keithley 2000 -> DMM; junk -> None.
- `Instruments.simulated().dmm.kind == DMM`; mapping includes DMM. Live `__init__` sets `self.dmm = self._open("DMM", required=False)`.
- `ate/tests/opa/stubs.py` does not exist. opa `__init__` imports mapped_dc + noise, not stubs.
- vohl lab_sheet=VOL, required `{DMM, PSU}`, run file=mapped_dc.py, no stub text. Runner formula `required_instruments - {MSO,PSU,AWG}` -> `['DMM']` (`runner.py` 922-926 returns `Missing instruments: ...`).
- psrr/cmrr/aol/emirr/power_on_time run files=mapped_dc.py, required MSO+PSU+AWG, no RuntimeError stub. noise run file=noise.py (0.1-10Hz body, not mapped_dc).
- `_run_case` captures JPEG when scope present and pastes if paste.photos exists. It does not implement PSRR/CMRR/AOL physics.

Cite:

- `ate/instruments/discovery.py` 71-88
- `ate/instruments/session.py` 13-35
- `ate/tests/opa/mapped_dc.py` 1-4, 42-102, 158-207
- `ate/tests/opa/__init__.py` 6-16
- `ate/core/runner.py` 922-941
- `ate/core/check_mapped_tests.py` 33-65, 94-105
- `ate/core/check_lab_report_sync.py` 94-104

R-0007: replaced vohl `frozenset({"PSU", "DMM"})` with `frozenset({"PSU"})`; `python -m ate.core.check_mapped_tests` -> EXIT:1 `vohl must require DMM`; restored needle; re-run EXIT:0. Gate can fail.

Classify-token poison (rename DMM6500 in `_DMM_MODEL`) did **not** go red: Keithley IDN still matches `"DMM" in u`. Not a ticket fail; the classify check is redundant for that IDN.

## A08 check does NOT prove

- USB Discover actually finding a DMM6500 on this bench.
- `spec.run()` itself raising if DMM is missing (body uses `_dmm_note` -> `dmm=off`; START gate is runner `required_instruments`).
- Full analog PSRR/CMRR/AOL (ticket allows leftover).
- Live START / Excel paste of those captures.
- stub detection is filename `.../stubs.py` only; a RuntimeError inside mapped_dc.py would not trip that clause (this file has none).

Leftover-honest (allowed, do not reopen as FAIL): PSRR/CMRR/AOL remain mapped screenshot captures in `mapped_dc.py`, not lab analog recipes.

# A09-T01 -- PASS

Acceptance: Logic campaign path; rs29511 Soo-only; rs1g08 Ariff DC ids; fixture LOGIC not G11; check_logic_campaign green.

Independent probes:

- Campaigns exist:
  - `C:\Users\OoiJianHong\#Test_Database\Logic\RS29511\SOIC\Soo\Version_1` (sheet_map present)
  - `C:\Users\OoiJianHong\#Test_Database\Logic\RS1G08\SOT23\Ariff\Version_1` (sheet_map present)
- rs29511 enabled: tp, tidle, tdis, ten, supply_current, output_voltage, cap_load. Intersection with Ariff DC ids = empty.
- rs1g08 enabled includes delta_supply_current, off_current, input_thresholds, ioff_leakage, input_leakage_sweep (plus vih_vil, voh_load, vol_load, supply_current_sweep, tp/tidle/tdis).
- `logic_catalog_for_ui` both parts: mode LOGIC only. No G11/BUFFER/G_NEG100 leak.
- Logic registry contains the Ariff DC ids. `ariff_dc.py` check in the module forbids `input()` and `import Ariff`.

Cite:

- `ate/config/parts/rs29511.yaml` 25-49
- `ate/config/parts/rs1g08.yaml` 51-86
- `ate/fixture/modes.py` 446-476
- `ate/core/check_logic_campaign.py` 109-166
- `ate/core/database.py` 843-874 (`find_campaign_root` Logic/Part/Package/Operator/Version)

R-0007: added `delta_supply_current` to rs29511 enabled_tests; check returned `rs29511 must not enable Ariff-only id delta_supply_current`; yaml restored (no delta_supply_current). Gate can fail.

## A09 check does NOT prove

- Live Setup Apply campaign / worker `list_tests` RPC (worker down).
- RS1G08 SC70-5 tree (check binds SOT23/Ariff).
- USB DMM/PSU actually running those DC bodies.
- Dual-stack `logic_tests.py` / `import Ariff.*` absence beyond `ariff_dc.py` text scan.
- RS0204 Level campaign (the check also covers A11 RS0204; this ticket's Logic claims do not depend on that extra).

# What this run is not

- Not a live operator-console e2e (8766/5174 forbidden).
- Not a close. Tickets stay implemented / R-0003 pending until Ticket Runner acts.
- Not a GitHub issue comment (these IDs are `docs/tickets/*.md`, no GH number in this repo).
