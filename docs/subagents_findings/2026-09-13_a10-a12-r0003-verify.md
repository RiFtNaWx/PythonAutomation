---
keywords: r-0003, a10, a11, a12, lim, rs2323, rs0204, ariff, leftover-honest, check_open_inventory, check_logic_campaign, check_family_load
main_idea: Independent R-0003 did not close tickets. A10 leftover-honest (rON still typ-only PDF image). A11 leftover-honest (Icc/VOH paste.values still None). A12 PASS. No FAIL for parent. All named checks exit 0. Worker/UI not started.
---

# 2026-09-13 A10/A11/A12 R-0003 verify (do not close)

PREFLIGHT: PARTIAL
reuse: docs/subagents_findings/2026-09-03_a10-lim-rs2323-open-inventory.md, 2026-09-03_a11-rs0204-campaign.md, 2026-09-03_a11-rs0204-dual-rail-bodies.md, 2026-09-04_a12-ariff-latest-wraps.md
spawn: skip (this agent is the independent verifier; no implementer edits)

Verifier did not change ticket Status. Did not edit product code. Did not start worker 8766 or UI 5174. Did not extract RS2323 rON from a PDF image. Did not invent Excel cells.

Shell cwd used junction `C:\Users\OoiJianHong\PythonAutomation` (same tree as `Eugene's Repo\PythonAutomation`) because the apostrophe in the folder name breaks the Cursor PowerShell wrapper. Python: `.\venv\Scripts\python.exe`.

## Verdict table

| Ticket | Verdict | Check | Exit | Leftover still true? |
|--------|---------|-------|------|----------------------|
| A10-T01 | leftover-honest | `python -m ate.core.check_open_inventory` | 0 | yes: RON_ohm typ 0.6 only, no min/max |
| A11-T01 | leftover-honest | `python -m ate.core.check_logic_campaign` | 0 | yes: Icc and VOH `paste.values` are None |
| A12-T01 | PASS | `check_logic_campaign` + `check_family_load` | 0 + 0 | none named on this ticket |

FAILs for parent to fix: **none**.

## Commands (stdout + exit)

```
python -m ate.core.check_open_inventory
OK open-inventory: AnalogSwitch RS2323 + Logic RS1G07/14 + Level RS0204 + Power RS3213
EXIT: 0

python -m ate.core.check_logic_campaign
OK logic-campaign: RS29511 + RS1G08 + RS0204 sheet_map / enabled_tests / LOGIC fixture / A12 Ariff ids
EXIT: 0

python -m ate.core.check_family_load
OK opamp=17 logic=34 level=34 switch=4 power=6 demo=1 restored=17 tests (no cross-family leak); family-scoped catalog/timing OK
EXIT: 0
```

## A10 claims vs disk

PASS on listed claims; leftover-honest because rON remains banner typ.

- Family package is still `ate/tests/lim/` but rail is Analog SW (`data-family="switch"`). `load_family("lim")` returns `switch`. Four ids registered with fixture `LIM_RS2323`: iplus, leakage_off, leakage_on, input_leakage. Cite `ate/tests/lim/rs2323.py` `_FIXTURE` + `_reg` blocks.
- Continue path: `_pause` calls `params.pause_hook`, never a real `input(` call. No `import Lim` / `from Lim`. Cite `_pause` in the same file.
- No OPA G11 on those specs: all four `fixture_mode=LIM_RS2323`. `check_open_inventory` also rejects G11/BUFFER/G_NEG100 on the switch registry.
- Campaign on disk is AnalogSwitch/RS2323/MSOP/SeeLim/Version_1 (ticket Files still says `#Test_Database/Lim/...`). Check looks for AnalogSwitch and passed. Not a FAIL of the four-test / Continue / no-G11 claims.
- Leftover: `ate/config/limits/rs2323.yaml` `RON_ohm` has `typ: 0.6` only. Extract `ate/config/datasheets/text/rs2323.txt` has banner "Low ON-State Resistance, 0.6" and empty Electrical Characteristics pages; table min/max not in text. Did not invent a table.

What `check_open_inventory` does NOT prove: a live Analog SW click, operator Continue on the console, DMM numbers, or rON min/max. It does not start UI. Headless `_pause` returns True when `pause_hook` is None.

## A11 claims vs disk

PASS on listed claims; leftover-honest because Excel grids are still unmapped.

- 16 TestSpecs in `ate/tests/logic/rs0204.py` `CASES`; lab_sheet set equals live workbook sheets VIH VIL VOH VOL Icc Il Tpd Tp Tsu Th fmax tr tf Tsk Cpd tw (Summary/Checklist ignored). Campaign: `#Test_Database/Level/RS0204/TSSOP14/ChangThong/Version_1`. sheet_map has 16 keys, all `excel_sheet` present on the xlsx.
- Dual-rail: `rails_from_params` raises if VCCA > VCCB. `_power_dual` does `power_on_protected(..., 1, vcca)` then `(..., 2, vccb)`. Cite those functions.
- No live `import Soo` / `from Soo` (only comments). Substring `Soo.logic_tests` absent. Id is `tp_rs0204` (sheet Tp), not RS29511 `tp` from `ate/tests/logic/wraps.py`.
- Fixture on all 16 is LOGIC, not G11. `check_logic_campaign` also refuses OPA modes in `logic_catalog_for_ui("rs0204")`.
- Leftover: every RS0204 sheet_map entry has `paste.values=None`, including Icc and VOH. Ticket leftover allowed. Did not invent cells.

What `check_logic_campaign` does NOT prove: live START with two PSU rails, operator wiring, Excel Fill of Icc/VOH, or a browser with G11 hidden. It does not enable-check rs1g32/rs1gt32d (A12). It matches workbook sheet names to sheet_map `excel_sheet`, not a full cell map.

## A12 claims vs disk

PASS. No named leftover on A12-T01.

- Logic load registers `supply_current_sweep`, `vih_vil`, `voh_load`, `vol_load` in `ate/tests/logic/ariff_dc.py`. Thickening present in source: `off_current` 8 combos; `ioff_leakage` pin-force combos; `input_leakage_sweep` YAML VCC list; `input_thresholds` step 5 on `vcc*100` (0.05 V) plus `HYSTERESIS_V`.
- Enabled on rs1g08 / rs1g32 / rs1gt32d (yaml `enabled_tests` and `catalog_for_ui`). Independent of `check_logic_campaign`, which only asserts rs1g08 for those four ids.
- rs29511 `enabled_tests` / catalog do not include those Ariff-only ids (Soo set: tp tidle tdis ten supply_current output_voltage cap_load).
- `ariff_dc.py`: no real `input(`; no `import Ariff` / `from Ariff` at statement start. Top-level imports are stdlib + yaml + ate.core only.

What the A12 checks do NOT prove: live checkbox clicks, 8-combo execution on hardware, Ariff Repo 3-PSU VIH path (platform uses PSU+AWG), or LDO wraps (parked). `check_family_load` does not grep `input(` (that is `check_logic_campaign` on ariff_dc.py).

## Tickets left open

- `docs/tickets/A10-T01-lim-rs2323.md` Status remains implemented (R-0003 pending)
- `docs/tickets/A11-T01-rs0204-campaign.md` Status remains implemented (R-0003 pending)
- `docs/tickets/A12-T01-ariff-latest-wraps.md` Status remains implemented (R-0003 pending)

Parent may mark A12 closed-accepted; A10/A11 stay leftover-honest until rON extract and Icc/VOH grids are real. This verifier does not close them.
