---
keywords: junior, g201, ac_gain, ac_vin, GAIN_VV, chan2, opa_tests, leftover-honest
main_idea: RS622 ac_gain_check and ac_vin_sweep now Path B on G201 CHAN2 (closed-loop GAIN_VV~201). Do not wrap opa_tests CHAN1=AWG. Not AOL_dB. Leftover 13 unchanged.
---

# G201 AC gain Path B (2026-09-15)

Golden `opa_tests.test_ac_gain_check` / `test_ac_vin_sweep` probe CHAN1 as OutB. On this bench CHAN1 is AWG VIN (same trap as VOS). Wrapping them stamped nothing, so SIM 218/218 hid GAIN~1.

## What changed

- `ate/tests/opa/ac_gain.py` -- Path B. Dual PSU, AWG CH1 sine 4 mVpp / 500 Hz, MSO CHAN2 VPP / amp_cmd = `GAIN_VV`. Flag `g201_ac`.
- `ate/tests/opa/ac_vin.py` -- same CHAN2, Vin DC sweep, stamp `GAIN_VV` mean. No research xlsx.
- `ate/instruments/sim.py` -- dual matched rails + AWG CH1 SIN `vpp<=10 mVpp` -> CHAN2 Av=201 1-pole. GBW 50 mVpp stays G11 Av~11.
- `ate/config/limits/rs622.yaml` -- `GAIN_VV` typ 201, no min/max (not AOL_dB).
- `physics_enabled.json` -- each row gets TestSpec `file`. REALIZED why on rs622 ac_*.

## Do not

- Stamp AOL_dB from G201 / BUFFER / G11.
- Reclassify leftover iso/xtalk/ron/settling/noise as REALIZED.
- Fake SIM GAIN as datasheet open-loop.

## Proof

```
python -m ate.core.check_add_test
python -m ate.core.check_sim_run
python -m ate.core.check_all_parts
python -m ate.core.physics_scale
```

SIM G201 CHAN2 GAIN_VV must be ~201, not ~1 (CHAN1) and not ~11 (G11).
