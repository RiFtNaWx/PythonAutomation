---
keywords: junior, rs2227, usb, dpdt, leakage, iplus, ioz, ion, iin, leftover-honest, path-b
main_idea: RS2227 iplus/leakage_off/leakage_on/input_leakage now branch USB DPDT Continue text (S/OE/HSD/D+/D-). Same TestSpec ids. Still no RS2323 ron/ton on rs2227. BW/ISO/XTalk leftover.
---

# RS2227 USB leakage Continue (not RS2323 COM/NO)

## What changed

`ate/tests/lim/rs2323.py` `_is_usb()` when `part_key == rs2227`:

- iplus: S=GND (HSD1) vs S=V+ (HSD2); OE=GND; CH1 through DMM to V+.
- leakage_off: OE=V+ Hi-Z; CH2 0.3 V via DMM into D+ then D-; no CH3 COM bias.
- leakage_on: OE=GND S=GND; ION on D+ (HSD1 ON) and HSD2+ (HSD2 OFF). No ICOM.
- input_leakage: IIN on S at V+ / 0, not IN1/NO/NC/COM.

Stamps stay `IPLUS_uA` / `IOZ_uA` / `ION_uA` / `IIN_uA`. USB rON stays `usb_ron`.

Check: `check_add_test` requires `_is_usb`, `OE=V+`, `IOZ D+`, `IIN S = V+`.

## Leftover-honest

- Analog-switch BW / ISO / XTalk (RS2227 550 MHz). SIM CH2 sine is G11 GBW, not an OFF switch.
- USB rON min/max still PDF image. tON/tOFF banner typ only.
- USB START of every SKU is not SIM.

## Proof

```
python -m ate.core.check_add_test
python -m ate.core.check_family_load
python -m ate.core.check_stimulus
python -m ate.core.check_specs_datalog
python -m ate.core.check_sim_run
python -m ate.core.check_all_parts
```

2026-09-15: `check_all_parts` SIM **226/226** yaml=26 stub=[] KEEP rs2227 includes iplus/leakage_*/usb_ron. Not USB START. Not junior-100%.
