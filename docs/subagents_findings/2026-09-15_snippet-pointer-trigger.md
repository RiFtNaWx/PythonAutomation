---
keywords: snippet-map, pointer, trigger, wrap, detect, imported-scaffold, leftover-honest, path-c, ast-clean, lim-ariff-soo, prd-003, a27
main_idea: A16 wrap still copies imported_<id>.py (stub, original not called). UI/UI_CONTRACT already paint Remember + located + source. PRD-003/A27 is pointer+trigger, the opposite of a no-code wizard. Do not reopen A16 tickets. list_tests.specs already reads limits yaml.
---

PREFLIGHT: PARTIAL
reuse: `docs/subagents_findings/2026-09-08_f23-detect-wrap-copy-version.md`, `2026-09-13_vibe-code-add-test-tickets.md`, `2026-09-13_campaign-tests-page.md`, A16 tickets
spawn: skip (prd-agent docs only)

## Nearness (artifact)

Tests `#page-detect` (`ate/ui/web/index.html` ~371) + `wrapSelectedDetected` (`app.js` ~4660) + `wrap_detected_test` (`ate/core/test_detect.py` ~432).

UI is ahead: Remember button, `res.located`, `res.snippet`, `t.source`. Engine still writes `imported_<id>.py` and returns `module`. `list_tests` has `specs` from `load_part_specs` (already yaml SoT) but no `source`. No `snippet_map.yaml`. `_blocked_reason` is `input()` only.

## Slice

- PRD-003: original SoT = Path B py + parts yaml + limits yaml + golden `file:lineno:fn`.
- EPIC-A27 READY. Tickets T01 map/scan, T02 wrap-trigger, T03 UI/docs honesty.
- A16 leftover-honest. Do not delete `imported_input_off_leakage.py` this wave.
- BAN: wizard, Monaco, `runner.py`, vendor live-import, n8n, TAS-POINTER HUD.

## Proof after implement (not this sitting)

Detect remembers file:line. Wrap of clean fixture does not write `imported_*.py`. START/DEMO runs original fn. Limits yaml change updates `list_tests.specs`. New `def test_*` appears on next scan.

## Not

Product code. A16 ticket reopen. A25/A26 in the same sitting (`test_detect.py` + Tests page).
