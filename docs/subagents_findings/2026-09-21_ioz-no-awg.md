keywords: ioz, awg, psu-ch3, rs1g126, missing-instruments, seelim, leftover-honest
main_idea: IOZ FAIL Missing AWG was Path B `_require_for` treating G126 AWG pin_drive as required. See Lin IOZ is PSU+DMM (CH3=OE). USB DG822 is on the bus this sitting; a live SeeLim START already maps AWG.

## Cause
`rs1g126.yaml` pin_drive A/OE = AWG. `_needs_awg` was true for every DC test. `_run_ioz` called `_require_for` then `_apply_levels(pin_drive)` so USB START died with `Missing instruments: AWG` when `instr.gen` was None (earlier PnP Unknown). Golden `test_ioz.required_instruments = {PSU, DMM}`.

## Fix (disk, not yet in the live worker)
- `_require_for`: skip AWG when tid is ioz
- `_ioz_drive_inactive`: PSU CH3 = OE inactive; never AWG
- stimulus / handoff / wire_map.tests.ioz: PSU CH1/CH2/CH3, no AWG

## Proof
- `python -m ate.core.check_stimulus` OK
- `python -m ate.core.check_logic_dc` OK
- visa_inventory USB *IDN PSU + AWG DG8Q281600755 + DMM + MSO; PnP OK includes DG8Q
- Live session_2026-09-21_151044 SeeLim RS1G126 ioz: instrument_map includes AWG; status running; steps []

## Leftover-honest
Worker busy on that START. Did not idle-restart. This run still loads the old module until STOP/finish + restart. Click Continue if the banner is up (LOGIC board, then DUT). Next idle-restart picks up no-AWG IOZ.
