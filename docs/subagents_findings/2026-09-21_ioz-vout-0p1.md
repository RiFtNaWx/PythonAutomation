---
keywords: ioz, vout-sweep, 0.1v, see-lin, rs1g126, n=2, leftover-honest
main_idea: Last START was n=2 because recipe ioz_vout_list was only 0 and 5.5. See Lin golden sweeps VO 0..5.5 step 0.1 (56 pts) at VCC=3.6. Path B now uses that grid.
---

# IOZ Vo 0.1 V sweep

Datasheet / See Lin `configurations.py`: `IOZ_VCC=3.6`, `IOZ_VOUT_START=0`, `STOP=5.5`, `STEP=0.10`.
Path B had `ioz_vout_list: [0.0, 5.5]` so STS showed `IOZ n=2`.

Fix: `ioz_vout_start/stop/step` on rs1g126/rs1g125 + `_ioz_vout_points` in `logic_dc.py`. Parameters expose Vout step. Two-point list no longer wins.

`unspec` on 2.74 uA vs max 10 is `settle=NON_TIGHT` (stable_eps_A null), not a missing limit. ICC 1.65-5.5 is a different TestSpec (`icc`).

Proof: `check_logic_dc` 56 pts; idle-restart worker; START IOZ again. Expect `n=56`.
