# A10-T01 - Lim RS2323 + open inventory

**Epic:** EPIC-A10
**Status:** closed-accepted (R-0003 leftover-honest: rON table still PDF image)
**Step:** current: 1 / 6

## Problem

Lim RS2323 lives only in LabAutomation-1 with hanging `input()` wiring prompts. Logic RS1G07/14 and Level have no `#Test_Database` campaigns. Platform claim needs every open slot fillable and checkable.

## Acceptance

WHEN Family Lim is selected, THE SYSTEM SHALL list iplus / leakage_off / leakage_on / input_leakage with fixture LIM_RS2323 and SHALL NOT show OPA G11 boards.

WHEN a Lim sub-condition needs rewiring, THE SYSTEM SHALL use operator Continue (pause_hook), never `input()`.

WHEN `python -m ate.core.check_open_inventory` runs, THE SYSTEM SHALL pass Lim + Logic RS1G07/14 + Level folder + family_load probes.

## Files

- `ate/tests/lim/`, `ate/config/parts/rs2323.yaml`
- `#Test_Database/Lim/RS2323/...`, Logic RS1G07/14, Level stub
- `ate/core/check_open_inventory.py`, registry/param_defaults/worker/UI
